---
interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<header class="studio-page-header">
  <h1>{title}</h1>
  {description && <p>{description}</p>}
  <slot />
</header>
